import Singleton from './singleton';
// import UI from './UI';

export default class Player {
  constructor() {

    // instances
    this.singleton = new Singleton();

    // elements

    // values
    this.random = false;
  }

}
